import {
  DashboardStats,
  DashboardActivity,
  DashboardRevenue,
} from "../shared/types";

// Re-export shared dashboard types
export type { DashboardStats, DashboardActivity, DashboardRevenue };
